# Capstone V2 랜딩 페이지

이 폴더는 `onit_v2/app` 빌드 결과물입니다.

## 업데이트 방법

캡스톤 랜딩 페이지를 수정한 후:

```bash
# frontend 디렉토리에서
npm run build:capstone:copy
```

또는 캡스톤 포함 전체 빌드:

```bash
npm run build:capstone
```

## 접근 경로

- `/capstone_v2` → 캡스톤 페이지로 리다이렉트
- `/capstone_v2/` → 캡스톤 랜딩 페이지 (직접 접근)

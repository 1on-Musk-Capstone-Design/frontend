# ON-IT 오프닝 씬 사용 가이드

## 📋 개요

three.js를 사용한 인터랙티브 오프닝 씬입니다. 다음 순서로 진행됩니다:

1. **격자 배경**으로 시작
2. **초록 공**이 하늘에서 떨어져 바닥에 닿음
3. **마우스 커서 힌트**가 나타남
4. **클릭**하면 화면이 옆으로 이동
5. **맥북 3D 모델**이 나타나서 두 바퀴 회전
6. 맥북 화면에 **"on-it"** 텍스트가 표시됨
7. 최종적으로 **캔버스 오버레이**가 나타남

---

## 🚀 빠른 시작

### 1. 파일 위치 확인

```
public/
├── opening-scene.html  (메인 파일)
└── assets/
    └── macbook.glb     (맥북 3D 모델 - 선택사항)
```

### 2. 브라우저에서 열기

**방법 1: 직접 열기**
```bash
# 파일 탐색기에서 직접 열기
open public/opening-scene.html
```

**방법 2: 로컬 서버 사용 (권장)**
```bash
# Vite 개발 서버 실행
npm run dev

# 브라우저에서 접속
# http://localhost:5173/opening-scene.html
```

**방법 3: Python 간단 서버**
```bash
cd public
python3 -m http.server 8000

# 브라우저에서 접속
# http://localhost:8000/opening-scene.html
```

---

## 📦 필요한 에셋

### 필수 에셋

**맥북 3D 모델** (선택사항)
- 위치: `/public/assets/macbook.glb`
- 모델이 없으면 기본 맥북 형태가 자동으로 생성됨
- 자세한 내용은 `ASSETS_NEEDED.md` 참고

---

## 🎨 커스터마이징

### 색상 변경

**초록 공 색상:**
```javascript
const ballMaterial = new THREE.MeshStandardMaterial({ 
    color: 0x01CD15,  // 이 값을 변경
    // ...
});
```

**격자 색상:**
```javascript
const gridHelper = new THREE.GridHelper(100, 100, 0x01CD15, 0x01CD15);
// 색상 값 변경
```

### 애니메이션 속도 조정

**공이 떨어지는 속도:**
```javascript
ballVelocity += 0.2;  // 값이 클수록 빠름
```

**화면 전환 속도:**
```javascript
transitionProgress += 0.015;  // 값이 클수록 빠름
```

**회전 속도:**
```javascript
rotationProgress += 0.008;     // 값이 클수록 빠름
macbookRotation += 0.016;     // 회전 속도
```

### 카메라 위치 조정

**초기 카메라 위치:**
```javascript
camera.position.set(0, 5, 10);
```

**맥북 회전 시 카메라 위치:**
```javascript
camera.position.set(-8, 3, 8);
```

---

## 🔧 문제 해결

### 모델이 로드되지 않을 때

**증상**: 콘솔에 에러 메시지가 나타남

**해결 방법**:
1. 모델 파일 경로 확인 (`./assets/macbook.glb`)
2. 파일이 실제로 존재하는지 확인
3. 모델이 없으면 기본 맥북 형태가 자동으로 생성됨

### 성능 문제

**증상**: 애니메이션이 끊기거나 느림

**해결 방법**:
1. 모델의 폴리곤 수 확인 (5,000-20,000 권장)
2. 브라우저 하드웨어 가속 확인
3. 다른 탭이나 프로그램 닫기

### CORS 에러

**증상**: 모델을 로드할 수 없음

**해결 방법**:
1. 로컬 서버를 사용하여 실행 (직접 파일 열기 X)
2. Vite 개발 서버 사용 권장

---

## 📝 코드 구조

### 주요 함수

**`createDefaultMacbook()`**
- 맥북 모델이 없을 때 기본 맥북 형태 생성
- 본체, 화면, 로고 텍스트 포함

**`animate()`**
- 메인 애니메이션 루프
- 상태에 따라 다른 애니메이션 실행

### 애니메이션 상태

- `falling`: 공이 떨어지는 중
- `bounce`: 공이 바닥에 닿아 바운스
- `wait`: 클릭 대기 중
- `transition`: 화면이 옆으로 이동
- `rotate`: 맥북이 회전
- `showCanvas`: 캔버스 오버레이 표시

---

## 🎬 시퀀스별 설명

### 1. 격자 배경 (0초)
- 무한 격자 패턴 표시
- 초록색 (#01CD15) 격자

### 2. 공이 떨어짐 (0-2초)
- 초록 공이 y=10에서 시작
- 중력에 의해 떨어짐
- 바닥(y=0.5)에 닿으면 바운스

### 3. 클릭 대기 (2-3초)
- 공이 바닥에 정지
- 마우스 커서 힌트 표시
- 클릭 대기

### 4. 화면 전환 (3-5초)
- 클릭 시 화면이 왼쪽으로 이동
- 맥북이 오른쪽에서 나타남
- 카메라가 맥북을 향하도록 이동

### 5. 맥북 회전 (5-7초)
- 맥북이 두 바퀴 회전 (720도)
- 카메라가 맥북 주위를 약간 회전
- 약 2초 동안 진행

### 6. 캔버스 표시 (7초 이후)
- 맥북 화면에 "on-it" 텍스트 표시
- 캔버스 오버레이가 페이드인
- 최종 캔버스 화면 표시

---

## 🎯 다음 단계

오프닝 씬이 끝난 후 실제 캔버스 페이지로 이동하려면:

```javascript
// opening-scene.html 마지막에 추가
setTimeout(() => {
    window.location.href = '/infinite-canvas'; // 실제 캔버스 페이지 경로
}, 10000); // 10초 후 이동
```

또는 사용자가 클릭하면 이동:

```javascript
document.getElementById('canvasOverlay').addEventListener('click', () => {
    window.location.href = '/infinite-canvas';
});
```

---

## 📚 참고 자료

- [Three.js 공식 문서](https://threejs.org/docs/)
- [GLTF 로더 문서](https://threejs.org/docs/#examples/en/loaders/GLTFLoader)
- [에셋 다운로드 가이드](./ASSETS_NEEDED.md)

---

**작성일**: 2024년 12월
**버전**: 1.0


